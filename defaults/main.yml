---
# Role default lower priority variables

# umask value. Default value is the CIS security guide recomendation

default_umask_value: "027"

# Define files where to apply the default umask value

default_umask_files:
  - /etc/profile
  - /etc/bashrc
